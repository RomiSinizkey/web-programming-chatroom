<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Register</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="container">
    <h1>Register - Step 1</h1>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form method="post" action="/register">
      <div class="form-row">
        <label>Email:</label>
        <input type="email" name="email" required maxlength="32"
               value="<%= data.email || '' %>"/>
      </div>

      <div class="form-row">
        <label>First name:</label>
        <input type="text" name="firstName" required minlength="3" maxlength="32"
               pattern="[A-Za-z]{3,32}" value="<%= data.firstName || '' %>"/>
      </div>

      <div class="form-row">
        <label>Last name:</label>
        <input type="text" name="lastName" required minlength="3" maxlength="32"
               pattern="[A-Za-z]{3,32}" value="<%= data.lastName || '' %>"/>
      </div>

      <button type="submit">Next</button>
    </form>

    <div class="actions">
      <a href="/">Back to login</a>
    </div>
  </div>
</body>
</html>
